# Changelog

本文件记录 OpenHPC WebUI 的重要功能变化。

## Unreleased

### Added

- 新增首次启动安装向导和管理员系统设置页，可配置 LDAP、Slurm、认证、文件管理、终端与日志参数；敏感凭据不向浏览器回显，保存后按配置类型即时重载或提示重启。
- 新增管理员维护的终端 AI 端点注册表，支持端点增删改；普通用户只能选择管理员允许的 Provider 和 Base URL，用户 API Key 不写入端点配置。
- 新增上传入口请求保护，可在 multipart 解析前按声明长度和实际接收字节限制请求体，并配置每进程并发上传数与处理超时。

### Changed

- 登录保护改为“来源地址 + 账户”组合限流和来源总量限流；用户名统一规范化，账户遭遇分布式失败时仅增加有上限的渐进延迟，不再全局锁死账户。
- 登录限流状态改为事务化 SQLite 存储，同一主机上的多个 worker 可共享失败状态，进程重启后仍然有效。
- LDAP、Slurm、配额和文件管理等同步集成统一移入线程池，并为 LDAP 连接、Slurm 命令及其他外部进程增加有界超时和明确的 503 错误。
- 作业输出改为从文件尾部按块读取，并同时限制返回行数和读取字节数；超长单行和超大日志不再触发完整文件扫描。
- 发布工作流固定 Node.js 版本，并在构建发行包前执行全部 `tests/*.js` 前端测试。

### Fixed

- 创建和删除用户现按 LDAP、Slurm、管理员权限及 NFS 配额的实际结果执行逆序补偿；失败响应会明确说明是否已经回滚或需要人工修复。
- 管理员列表改为加锁、临时文件写入、`fsync` 和原子替换，持久化失败时不再提前改变当前进程权限。
- Nginx 示例和部署文档将请求体上限调整为 1025 MiB，与默认 1024 MiB 单文件上限一致并预留 multipart 开销。
- CSV 作业报表使用标准 CSV 序列化，并将可能被电子表格解释为公式的单元格强制作为文本处理。

### Security

- `AUTHORIZED` 等布尔配置只接受明确的真值和假值，非法值会阻止启动，避免配置拼写错误静默关闭认证。
- 管理页面统一转义 LDAP、Slurm 和配置数据，动态操作不再把外部值拼入 HTML 或内联事件处理器，修复持久型 XSS。
- 作业详情、资源监控、输出读取和取消统一要求管理员或作业所有者权限，避免普通用户读取他人的命令和路径。
- LDAP 用户和组标识符统一使用字符白名单；查询过滤器使用 `escape_filter_chars()`，DN 的 RDN 使用 `escape_rdn()`，避免 Filter/DN 注入。
- 上传请求在业务处理前实施请求体、并发和超时限制，同时保留文件内容大小校验，降低绕过代理后耗尽临时磁盘的风险。

## 0.3.1 - 20260916

### Added

- Web 终端新增用户 AI 配置入口，可自行启用 AI、选择 Provider，并配置 Base URL、模型、API Key 与超时；默认仅在当前服务端会话保存，也可选择使用 AES-GCM 加密持久化到本地浏览器并在重连时恢复，切换端点时不会复用旧密钥。
- Slurm 用户关联新增分区访问状态，区分全局开放、分区限制、被全局关联覆盖及账户级关联；创建时从现有分区中选择，并提示删除全局关联可能取消运行中或排队的作业。
- 新增独立的“Slurm 用户管理”页面，按用户名汇总当前集群的账户和分区关联，支持搜索、创建用户、修改默认账户，以及确认后移除用户在当前集群中的全部关联；展示 Slurm 管理级别，默认账户仅可选择已关联账户。
- 补充 Slurm 用户管理使用说明、接口权限与命令行为测试；读取失败时显示服务错误，避免将故障误报为空用户列表。
- 节点添加和编辑新增 `NodeAddr`、`Parameters`、`State` 配置字段，支持保存、回读、更新及清空，可完整录入包含 GPU 和 NUMA 参数的节点配置。
- 新增基于 xterm.js 的 Web 终端，通过 WebSocket 连接后端 PTY，并将 xterm.js、FitAddon 和样式资源随应用离线打包。
- 新增 `TERMINAL_ENABLED`、`TERMINAL_IDLE_MINUTES` 和 `TERMINAL_MAX_SESSIONS` 配置，用于控制终端入口、空闲超时及单用户并发会话数。
- 新增 Nginx HTTPS/WebSocket 示例配置，以及终端使用、部署和故障排查文档。
- 新增终端 AI 对话，支持 OpenAI、Claude、GLM、DeepSeek、vLLM、SGLang 及其他 OpenAI 兼容接口，并可由管理员在系统设置中维护模型、地址、密钥和超时。
- 新增 Shell 命令与自然语言分流；AI 建议命令必须按 Ctrl+Enter 确认，执行完成后自动分析退出码与输出。
- 新增终端命令行 AI 输入提示、多轮上下文和“新对话”操作；重置 AI 对话不会影响当前 Shell。
- 新增 AI 文件动作，可预览并确认写入脚本或文本文件；兼容从未按 JSON 协议返回的 Markdown fenced 脚本中恢复文件动作。
- 新增终端 AI 目标循环：每个动作执行后自动分析结果并规划一个下一动作，直至目标完成；单次循环默认上限为 10 步，可在当前连接内调整为 1–50 步。
- 新增会话级“自动批准”开关；启用前显示安全风险，启用后可连续执行普通 AI 动作，关闭连接或开始新对话时自动复位。
- 新增终端当前模型显示与会话级模型选择器，支持从服务商建议项选择或手动输入兼容模型 ID；切换时自动重置 AI 上下文和自动批准，不影响 Shell 或其他用户。
- 将终端当前模型组合下拉框移至标题栏循环步数旁，保留建议模型选择和手动输入模型 ID。
- 新增可配置的 Web 终端顶部公告，管理员可设置公告内容、文字与背景颜色及加粗效果，并在设置页实时预览；公告以纯文本安全显示。

### Changed

- 明确 Slurm 默认账户修改入口：用户行按钮改为“修改默认账户”，关联页新增直达入口，并区分默认关联、默认 QOS 与列表筛选账户。
- 将原“集群用户”菜单、页面标题、面包屑及相关文档统一命名为“Slurm 用户关联”，与新增的“Slurm 用户管理”区分；分区、QOS 和计算额度继续通过关联页面管理。
- 节点列表改为勾选后统一操作，支持全选、取消选择和已选数量显示；上线、下线、删除支持批量确认并逐个执行，显示成功与失败数量，失败节点保留勾选以便重试。编辑仅在单选且存在对应配置时可用，删除要求所选节点均有对应配置。
- Supervisor 示例及安装文档统一使用 `/srv/openhpc-webui` 和 `.venv`，Python 包名、命令行入口及 Supervisor 程序名沿用 `openhpc_webui`，并补充现有部署的路径修复与重载步骤。
- Web 终端侧边栏改为当前页与新标签页双入口。
- 门户管理员的终端初始目录固定为其同名 Linux 系统用户的 Home，不沿用管理员文件管理的 `/` 或 `/root` 范围。
- 门户管理员进入文件管理时默认打开自己的 NSS/SSSD Home，“家目录”按钮也返回该目录，同时保留系统根目录访问能力。
- 终端连接期间切换站内页面时，可选择留在终端、在新标签打开目标，或断开 Shell 后离开。
- 新增终端悬浮收缩模式；收起后 WebSocket 和 Shell 保持运行，展开时自动重新适配终端尺寸。
- 刷新、关闭页签或通过地址栏离开活动终端时增加浏览器离开确认。
- 账户入口扩展为账户设置页；模型 API Key 仅服务端保存且不向浏览器回显。

### Fixed

- 全局关联更新和删除显式使用空分区选择条件，避免误操作同一用户和账户的其他分区关联；分区作为关联标识不可直接修改。
- 创建和更新关联时校验默认 QOS 必须属于显式提供的允许列表，页面同步限制可选默认 QOS；保留空列表继承和清除默认 QOS 的语义。
- 修复未设置 TRES 分钟限额时剩余额度被误报为零的问题，保留无限额度状态。
- 节点和分区配置改为原子写入，并在 `scontrol reconfigure` 失败时回滚，避免失败操作遗留配置。
- 删除 Slurm 用户关联失败时记录并回传命令的具体错误；默认账户关联删除被拒绝时返回冲突提示，引导先修改默认账户，避免仅显示通用 500 错误。
- 修复节点配置解析和更新时对行尾注释的处理，避免将注释中的字段识别为有效配置，或将新增字段写到注释后导致配置不生效。
- 补充 Nginx WebSocket `Upgrade` 和 `Connection` 转发配置，避免 `/ws/terminal` 被代理为普通 HTTP GET 并返回 404。
- 断开终端或关闭页面时主动回收 PTY 文件描述符和 Shell 进程，避免残留会话。
- 隐藏 AI 命令执行使用的内部包装与完成标记，并避免模型总结复述这些实现细节。
- 修复 bash/zsh bracketed paste 包装导致粘贴的自然语言被误交给 Shell 并报 `command not found`。
- 修复中文输入法或粘贴将文本与回车合并为一个终端事件时，自然语言未经过 AI 分类而被 Shell 执行的问题；同时兼容分段到达的 bracketed-paste 内容。
- 修复终端行跟踪在方向键、补全或特殊输入后永久失效的问题，并兼容模型误将“建议命令”写入分析文字的响应，避免提前结束目标循环。
- 修复终端 AI 重复询问已知的 Slurm、登录节点和用户名，以及只描述脚本却误报目标完成的问题；为缺失动作的响应增加一次自动纠偏，并在纠偏失败时保留原始目标。
- 修复部分推理/兼容模型返回空正文、分段正文或“说明文字 + JSON”时动作丢失的问题；空正文自动重试，混合响应提取协议 JSON，并约束资源申请和轻量测试。终端 AI 预设当前为 Slurm 集群登录节点，但不预设分区、节点、GRES/TRES、硬件厂商或固定参数，所有站点配置均从当前环境动态查询。
- 修复模型将待执行命令放入独立 Shell 代码块时目标被错误结束的问题，并在 AI 响应、异常及重连后恢复自然语言输入跟踪，避免后续中文问题被直接交给 Shell。

### Security

- `SLURM_CLUSTER_NAME` 现为必填配置；账户、用户关联及额度操作限定到配置集群，缺失或非法集群名时拒绝写入。升级前须核对该配置与 `sacctmgr list cluster` 中的名称一致。
- 作业详情、监控和取消接口在执行 Slurm 命令前校验作业 ID，拒绝选项形式的输入。
- Slurm 用户管理页面及接口仅管理员可访问，增删改命令校验用户名、账户名与集群配置；删除限定为当前集群，并提示全部关联及作业影响，默认账户编辑提示共享计费数据库下的跨集群影响。
- 为节点配置的 `NodeAddr`、`Parameters`、`State` 字段增加输入校验，拒绝空白、控制字符、注释标记、引号和反斜杠，避免输入改变配置行结构。
- WebSocket 复用门户登录会话并校验请求来源，拒绝未认证、跨站来源及已禁用账户。
- Shell 始终降权到当前同名 Linux 系统用户，不继承门户的 LDAP 凭据、Session 密钥等敏感环境变量。
- 增加系统账户、Home、登录 Shell、UID 唯一性和进程切换权限检查，拒绝 UID 与其他用户名冲突的终端会话。
- AI 建议命令仅暂存在当前 WebSocket 会话；默认逐步人工确认，自动批准模式对提权、递归删除、磁盘操作、关机及取消作业等高危命令仍强制确认，并对自定义循环步数实施服务端上限。
- AI 执行输出按不可信数据提交给模型，提示模型不得遵循输出中嵌入的角色或操作指令；模型配置接口仅管理员可写，API Key 在审计日志中脱敏。

## 0.3.0 - 20260830

### Added

- 新增文件管理页面，支持目录浏览、分段加载、上传下载、创建目录、重命名和递归删除。
- 新增隐藏文件显示开关，并为超大目录提供 50、100、200 条可选分页。
- 新增 UTF-8 文本文件在线编辑器，支持同步行号、文件变更检测和未保存退出确认。
- 新增“家目录”快捷入口；普通用户返回其 LDAP Home，管理员优先进入 `/root`。
- 新增 `FILE_UPLOAD_MAX_MB` 和 `FILE_EDIT_MAX_KB` 配置，分别限制上传和在线编辑文件大小。
- 新增版本号同步脚本及版本调整、GitHub Release 和 PyPI 发布文档。

### Changed

- 文件点击行为改为打开在线编辑器，不再直接触发下载。
- 文件操作、路径跳转及未保存确认统一使用站内弹窗，并调整全局按钮、路径导航和操作列表样式。
- 文件系统操作移至线程池执行，目录列表使用流式分页，避免超大目录阻塞请求或占用过多内存。

### Security

- 普通用户仅能访问其 LDAP Home 范围内的路径，管理员以 root 权限访问系统路径。
- 增加路径穿越和符号链接越界防护，并将普通用户创建或上传的内容归属到对应 LDAP UID/GID。

## 0.2.1 - 20260830

### Added

- 在作业管理操作列新增“监控”入口。
- 新增作业 CPU、内存、磁盘 I/O、GPU 利用率与 GPU 显存的实时监控弹窗。
- 新增 CPU/GPU、内存和累计磁盘 I/O 的 SVG 趋势图，支持 15 分钟至 2 小时的显示范围。
- 将作业监控采样临时缓存在浏览器中：每 5 秒采样，最长保留 2 小时，单作业最多 1440 个采样点，最多保留 8 个作业。
- 新增作业步骤资源占用明细，以及立即刷新和清空浏览器历史功能。

### Changed

- 作业监控弹窗改为响应式图表布局，步骤明细默认折叠。
- CPU 与 GPU 利用率统一限制在 0–100% 范围。
